
useParams() is a React Router hook that lets a component read URL parameters.
------------

useNavigate() is a React Router hook that programmatically changes routes without reloading the page.
-------------